1.创建文件并输入第一行
创建文件：1.创建文件并输入第一行
创建文件：vim ssh_list.txt
输入第一行：按 i 进入插入模式，输入ssh root@10.64.198.1 > 1.log
按 Esc 返回普通模式

2.录制宏
按 qa 开始录制宏
yy:复制当前行
p:在下一行粘贴
0:光标移动到行首
连按9下w：光标移动到98.1的1下
Ctrl+a:将光标下的数字加1
按 q 停止录制

3.执行宏
按 48@a 连续执行48次，可生成到第50行

4.切换日志文件
光标移到第50行，按 yyp 复制并粘贴出第51行
将第51行的 1.log 改为 2.log：光标移到数字1上，按r再按2
继续按 49@a 生成到第 100 行
重复以上步骤，依次将日志改为 3.log、4.log、5.log、6.log，直到第 255 行

5.保存文件
:wq保存并退出
